:toc:

= AIDIR

== TODO

=== dunning: drive the subscription lifecycle from a real payment provider
Quota usage is settled - feature_grant holds one row per numeric entitlement, and
SubscriptionService composes every lifecycle event out of openPeriod/closePeriod. What is missing
is the scheduler that decides when to call them: there is no payment, invoice or provider
integration yet, so renew/beginGrace/recoverFromGrace have nothing to charge against and are
currently only reachable by hand.

=== on product details page, allow vendors to link each pricing tier to its own dedicated sales page (feature gated by vendor package level)

=== [FE] when onboarding, if user attempts to advance to next step but cannot because of validation errors, those errors need to be displayed (currently not the case for at least CompanyInformationStep)

=== [FE] split user onboarding into two flows based on free/talent tier selection; free tier only has the create account step, talent tier also has to upload/fill-in CV (+ payment)

=== sign up with... options
What 3rd party signup options should we offer for users? Google, GitHub, FB?

=== marketing metrics
(marketing metrics best practice/most useful)

=== update model with new fields:
==== Key Features
array of strings

==== Media
multiple images and one video

==== faq
list of string pairs (q, a)

=== api for details page:
==== generate alternatives for details page

==== get pricing information from somewhere?

=== sorting for search results
Answered for now by `/api/tool/browse`, which sorts by relevance, newest or price. Rating is the obvious
missing dimension and waits on the entry below; "popular" waits on the view/click counters under
featured and trending signals.

=== store rating information
==== rating per user per tool
==== compute average rating for tools
Nothing in the schema holds a rating. Until it does, the directory cards draw their stars from
`src/components/search/placeholders.ts` on the front end and the rating rows in the filter sidebar render
inert - the sort dropdown deliberately does not offer Rating, because ordering the catalogue by a
stand-in value looks like a bug rather than a placeholder. That file and the sidebar's Rating group are
the two call sites to remove when this lands.

=== store search/query metrics
=== paginated search results
Done for the directory: `/api/tool/browse` takes page and size, and answers with a total count and the
category and pricing facet counts for the filters. `/api/search` is still the capped, unpaged endpoint
behind the autocomplete, which is all it is used for now.

=== featured and trending signals
The directory has Featured and Trending tabs, and the navigation has a Verified entry, with no data
behind any of them: there is no featured flag on a tool, no view or click counters, and no verified
column. Both tabs currently fall through to the newest listings and the Verified link opens the
unfiltered directory. Needs the counters (which pairs with "store search/query metrics"), a curation flag
for featured, and verification wired to the vendor tier that sells it.

=== talent signals the schema has nowhere to put
`/api/talent/browse` serves the half of the directory's sidebar that has columns behind it - skills,
languages, workplace, employment type, location, relocation and text. The other half is rendered disabled
on the front end, with its stand-in values quarantined in `src/components/talent/placeholders.ts`. The
same file now also feeds the public profile page, which draws the badges, the availability dot, the years
of experience, the industries row and a last-updated date whether or not anything backs them:

* open-to-work, and the response-rate signal behind "More likely to respond" - no flags, nothing recording
  outreach replies
* verified and featured - the same gap as tools; see "featured and trending signals"
* industries and talent tags - tools have a `tags` JSONB, talent has neither column nor vocabulary
* years of experience - `talent_experience.period` is free text like "2021 - Present", so it is neither
  filterable nor sortable. This is what blocks the Experience sort, not just the min/max filter
* title scope (current vs past role) - `talent_experience` has no current-role flag and no parsed dates
* seniority, and match score for the "AI Match" badge and its default sort - nothing computes either
* profile photos - there is no talent image table; only tools have `tool_image_metadata`, and onboarding
  never accepts a photo
* last updated - `talent_profile` records `created_at` only, so the profile page's "Updated" line is
  derived from the creation date rather than from anything that tracks edits
* daily rates - the rate insights section on the profile page is entirely front-end mock; nothing collects
  rates and nothing aggregates comparable ones

Also: `work_employment_type` is a single VARCHAR while the directory's design treats engagement types as a
set a person accepts. And a talent directory is personal data served with no auth gate, which mattered
less when the only public listing was a catalogue of products.

=== apply vendor entitlements to ranking
`vendor_search_boost`, `vendor_priority_listing`, `vendor_top_3_ranking` and `vendor_category_featured`
exist as features with grants behind them, but no code consults `feature_grant` when searching or
listing: the paid placement is sold and recorded and then not applied. Both `DocumentSearchService` and
the ordering in `ToolBrowseService` would have to read it.

=== persist the lucene index
`LuceneIndexManager` builds a `ByteBuffersDirectory`, so the index lives in the heap, dies with the JVM
and cannot be shared between nodes - every restart serves nothing until `SearchIndexBootstrap` has
rebuilt it. `/api/tool/browse` was written not to depend on it for filtering, counting or paging for that
reason, but the text query still does.

=== filters written into the search input
The original todo in `DocumentSearchService.search(SearchRequest)`: let the query itself carry filters,
`#cat:coding` and the like. `addExactTagFilters` and `buildCategoryQuery` are already written there and
already unreachable - no endpoint calls the overloads that use them. The front page search bar already
parses `#cat:` client-side and turns it into a category parameter, so the two need to agree on the syntax.

=== validate submitted categories against the category table
`VendorOnboardingService` writes whatever categories the payload carries straight into
`tool_submission.categories` as free text. The `category` table is now seeded and is what the directory
filters on, so a typo or a stale front-end list silently produces a tool nobody can filter to - which is
exactly how the front end ended up with two disagreeing category lists. `tool_submission_category` is
the join table for this and is still never written to.

=== auth
==== Add Spring security
==== add login support
==== sign in with... support

=== how to track social media shares?

=== Audit tables

=== Data retention

=== Consider caching strategy
- both for directus and pg

=== Set up development server
==== Pricing
[cols="1,1,1,1,1,1,1"]
|===
|Provider |CPU | RAM| Storage| Traffic |Price/m (with tax)|Link

|DigitalOcean |2v | 4 | 80 |4 TB |$29 (~€25) |https://cloud.digitalocean.com/droplets/new?i=db48cd&fleetUuid=fb640037-9297-4ddc-9ff4-6862eca5fe55&distro=ubuntu&distroImage=ubuntu-24-04-x64&region=fra1&size=s-2vcpu-4gb

|Hetzner
|6@3.6Ghz
|64
|2x512 NVMe
|?
|€45
|https://www.hetzner.com/dedicated-rootserver/

|Netcup
|4v
|4
|128
|free+throttle
|€4.84
|https://www.netcup.com/en/server/vps/vps-500-g11s-iv

|OVHcloud
|4v
|8
|75 Nvme
|unlimited
|€4.47
|https://www.ovhcloud.com/en/vps/configurator/?planCode=vps-2025-model1&brick=VPS%2BModel%2B1&pricing=upfront12&processor=%20&vcore=4__vCore&storage=75__SSD__NVMe

|Ionos
|6v
|8
|240 NVme
|unlimited
|€5.77 for 24 months
|https://www.ionos.co.uk/servers/vps
|===

=== look for loyalty programme framework


=== Find out how to serve static pages from external directory

=== Check directus/strapi video link support

=== Design initial database tables
For this phase, just the minimum amount of structure that would support phase 0 features.

==== User accounts
[square]
* registration
* basic log in
* roles
* basic profile information

==== Tool submission
Q: should the approved submissions be stored in the CMS?

[square]
* review queue
* table for approved listings
** id
** name
** url
** pricing (enum: free|freemium|paid)
** short description (20-30 words)
** long description
** category (for hierarchical display)
** tags (indexed jsonb array)
** image (maybe cms object id)
** ratings
** submitted by
** approved by
** approved at
** last verified
* worth storing rejected submissions?

=== Locally hosted Wiki option
for docs and bringing people up to speed

https://www.bookstackapp.com/
https://js.wiki/
https://github.com/docmost/docmost

==== Paid listings
Q: how are we going to accept payments?

[square]
* table for tracking paid listings
** campaign id
** listing id (fk)
** created
** expires
** expired? (bool flag for quick filtering)


==== Taxonomy (categories/tags)
Should be covered by listing table.

==== Newsletter
Users/emails that subscribe to the newsletter should be added to a table, support easy unsubscribe. Assuming we won't require an account for subscribing to the newsletter, the email address is basically the only piece of data we need to store.

* table
** email

==== Analytics
Q: look into some off the shelf solutions for this

==== Languages
Multilingual support should hopefully be covered by the CMS, a lot of decisions hang on where we store most of the site data (and the CMS obviously makes sense as a general solution)

=== Evaluate CMS options

Since this is one of the core features of the website, choosing the right CMS is going to be an essential decision. Most likely it will run in headless mode: the site will retrieve document information via a REST api and populate whatever custom components we define.

It'll also be used for the blog and the newsletter. While the blog is part of the site and will use the same rendering strategy as the other documents, the newsletter will probably use whatever the CMS supports, so I need to weigh its document rendering features.

Can the CMS push notifications to the backend when a new document is created, so the backend is aware? (listings, for example, blog posts etc.)

==== Candidates:

===== Directus

===== Strapi

===== Bloomreach




== DONE
